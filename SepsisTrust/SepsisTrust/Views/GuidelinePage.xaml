<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SepsisTrust.Views.GuidelinePage"
             Title="{Binding Block.Title}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="MainStackLayoutStyle" TargetType="StackLayout"
                   BasedOn="{StaticResource DefaultStackLayoutStyle}">
                <Setter Property="Spacing" Value="0" />
            </Style>

            <Style x:Key="TemplateViewStyle" TargetType="ContentView">
                <Setter Property="HorizontalOptions" Value="FillAndExpand" />
                <Setter Property="VerticalOptions" Value="FillAndExpand" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>
        <ContentView Content="{Binding Template}" />
        <Button Text="{Binding ProceedButtonText}" Command="{Binding ProceedCommand}" Style="{StaticResource BottomPageButtonStyle}" />
    </StackLayout>

</ContentPage>