<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SepsisTrust.Views.GuidelinePage"
             Title="{Binding Block.Title}">

    <StackLayout>
        <ContentView Content="{Binding Template}" />
        <Button Text="{Binding ProceedButtonText}" Command="{Binding ProceedCommand}" Style="{DynamicResource BottomPageButtonStyle}" />
    </StackLayout>

</ContentPage>